/*
 * Database Package - Content Automation Platform FASE 1
 * Prisma ORM schema foundation
 * Multi-tenant ready with tenant_id on all relevant tables
*/

export const DATABASE_PROVIDER = 'postgresql';

/** Extracts the Prisma schema path relative to this package. */
export const PRISMA_SCHEMA_PATH = 'prisma/schema.prisma';
